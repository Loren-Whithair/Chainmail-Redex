#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-addr-in-fieldMap)

(define (test-addr-in-fieldMap)

  (display "-------------------------------------")
  (display "\nRunning addr-in-fieldMap Tests:\n")

                        
  (test-results)
  (display "-------------------------------------")
  )

