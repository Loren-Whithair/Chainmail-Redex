#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-addr-in-lcl)

(define (test-addr-in-lcl)

  (display "-------------------------------------")
  (display "\nRunning addr-in-lcl Tests:\n")

                        
  (test-results)
  (display "-------------------------------------")
  )

