#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-lcl-addr-name)

(define (test-lcl-addr-name)

  (display "-------------------------------------")
  (display "\nRunning lcl-addr-name Tests:\n")

                        
  (test-results)
  (display "-------------------------------------")
  )

