#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-get-classname)

(define (test-get-classname)

  
  (display "-------------------------------------")
  (display "\nRunning varAssgn_OS Tests:\n")


  ;;PUT TESTS HERE
  
  (test-results)
  (display "-------------------------------------")

  )

