#lang racket
(require redex)

(provide test-lcl-lookup)

(define (test-lcl-lookup)

  (display "-------------------------------------")
  (display "\nRunning lcl-lookup Tests:\n")


  (define true_lcl-lookup (list

                        ))

  (define false_lcl-lookup (list

                         ))

  (for ([lcl-lookup true_lcl-lookup])
    (test-equal )
  
  (for ([lcl-lookup false_lcl-lookup])
    (test-equal )

  (test-results)

  (display "-------------------------------------")
  )