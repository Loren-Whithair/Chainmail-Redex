#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-method-Stmts)

(define (test-method-Stmts)

  (display "-------------------------------------")
  (display "\nRunning method-Stmts Tests:\n")
               
  (test-results)
  (display "-------------------------------------")
  )

