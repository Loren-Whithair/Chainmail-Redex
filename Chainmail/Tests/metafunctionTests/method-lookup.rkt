#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-method-lookup)

(define (test-method-lookup)

  (display "-------------------------------------")
  (display "\nRunning method-params Tests:\n")
               
  (test-results)
  (display "-------------------------------------")
  )

