#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-method-params)

(define (test-method-params)

  (display "-------------------------------------")
  (display "\nRunning method-params Tests:\n")
               
  (test-results)
  (display "-------------------------------------")
  )

