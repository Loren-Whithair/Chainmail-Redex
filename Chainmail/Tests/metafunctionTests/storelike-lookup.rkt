#lang racket
(require redex)

(provide test-storelike-lookup)

(define (test-storelike-lookup)

  (display "-------------------------------------")
  (display "\nRunning _ Tests:\n")


  (define true_storelike-lookup (list

                        ))

  (define false_storelike-lookup (list

                         ))

  (for ([storelike-lookup true_storelike-lookup])
    (test-equal )
  
  (for ([storelike-lookup false_storelike-lookup])
    (test-equal )

  (test-results)

  (display "-------------------------------------")
  )