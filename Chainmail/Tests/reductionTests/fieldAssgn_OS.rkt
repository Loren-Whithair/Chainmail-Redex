#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-fieldAssgn_OS)

(define (test-fieldAssgn_OS)

  (display "-------------------------------------")
  (display "\nRunning fieldAssgn_OS Tests:\n")


  ;;PUT TESTS HERE
  
  (test-results)
  (display "-------------------------------------")
  )