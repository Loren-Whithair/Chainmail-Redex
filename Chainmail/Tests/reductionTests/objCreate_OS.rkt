#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-objCreate_OS)

(define (test-objCreate_OS)
  
  (display "-------------------------------------")
  (display "\nRunning objCreate_OS Tests:\n")


  ;;PUT TESTS HERE
  
  (test-results)
  (display "-------------------------------------")
  )