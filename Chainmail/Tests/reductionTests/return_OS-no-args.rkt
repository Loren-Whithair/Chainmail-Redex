#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-return_OS-no-args)

(define (test-return_OS-no-args)

  (display "-------------------------------------")
  (display "\nRunning return_OS-no-args Tests:\n")


  ;----------------
  ;---TRUE TESTS---
  ;----------------

  ;----------------
  ;---FALSE TESTS--
  ;----------------

  
  (test-results)
  (display "-------------------------------------")
  )