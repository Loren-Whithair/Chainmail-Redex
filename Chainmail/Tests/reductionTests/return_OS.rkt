#lang racket
(require redex)
(require "../../Loo.rkt")

(provide test-return_OS)

(define (test-return_OS)

  (display "-------------------------------------")
  (display "\nRunning return_OS Tests:\n")
  
  ;----------------
  ;---TRUE TESTS---
  ;----------------

  ;----------------
  ;---FALSE TESTS--
  ;----------------

  (test-results)
  (display "-------------------------------------")

  )