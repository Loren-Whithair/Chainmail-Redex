#lang racket
(require redex)
(require "../../../Loo.rkt")
(require "../../../Chainmail.rkt")

(provide test-chainmail-implies)

(define (test-chainmail-implies)

  (display "-------------------------------------")
  (display "\nRunning Chainmail 'implies' Tests:\n")


  (test-results)
  
  (display "-------------------------------------")
  )